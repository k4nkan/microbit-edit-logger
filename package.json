{
  "name": "microbit-edit-logger",
  "private": true,
  "scripts": {
    "init:target": "node scripts/init-pxt-target.js",
    "build": "npm run init:target && pxt install && pxt build",
    "serve:editor": "python3 -m http.server 8080 -d editor"
  },
  "dependencies": {
    "pxt-microbit": "^8.1.20"
  },
  "devDependencies": {
    "pxt": "0.5.1"
  }
}
